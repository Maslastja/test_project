<!DOCTYPE html>
<html>
	{% include "head.html" %}
	<body>
		{% set subname="sub9" %}
		<div class="container">
			<div class="columns">
				<div class="column col-ml-auto">
					{% include "base.html" %}
				</div>
				<div class="column col-8 col-mr-auto">
					<h1>Количество студентов</h1>
					<table  class="table table-striped table-hover">
						<tr>
							<th>группа</th>
							<th>количество студентов</th>
							<th>староста</th>
						</tr>
						{% for s in req %}
							<tr>
								<td>{{ s.grname }}</td>
								<td>{{ s.kolvo }}</td>
								<td>{{ s.star }}</td>
							</tr>
						{% endfor %}
					</table>    
				</div> 
				<div class="column col-ml-auto">
					{% if 'user_id' in session %}
						<span class="label label-secondary">пользователь: {{ session['username'] }}</span>
					{% endif %}
					<a name='exit' href='/login' class="btn-link">Выход</a> 
				</div>
			</div>
		</div>
	</body>
</html>
